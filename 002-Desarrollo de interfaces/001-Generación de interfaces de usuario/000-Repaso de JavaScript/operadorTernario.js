const edad = 20;


const mensaje =
    edad >= 18
        ? "Mayor de edad"
        : "Menor de edad";


console.log(mensaje);

const activo = true;


const estado =
    activo
        ? "Activo"
        : "Inactivo";


console.log(
    `Estado del usuario: ${estado}`
);